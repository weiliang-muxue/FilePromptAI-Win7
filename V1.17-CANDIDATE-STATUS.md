# FilePrompt AI v1.17 候选状态

更新时间：2026-08-24

本文件记录当前提交的实际完成边界。本提交是 **v1.17 候选源码**，按维护者要求直接
提交，未在提交前重新运行完整构建或测试；它不是正式发布声明，也不代表已经通过
Windows 7 目标机验收。

## 已完成（代码已实现，本次未重新验证）

- 可从用户填写的 OpenAI Chat Completions 兼容地址推导并主动请求同源 `/models`，
  对结果去重、排序并提供下拉选择，同时保留手工输入模型名称。
- API Key 可留空；匿名模型发现、连接测试和聊天请求不发送 `Authorization`。
- 模型请求、模型列表请求和 HTTP MCP 显式禁用 Windows 系统代理，只连接用户填写
  或由该地址推导出的端点。
- 新增普通 system prompt，以及可独立启停的 `temperature`、`top_p` 和最大输出
  token；设置和模型预设均可保存这些字段。
- 自定义 system prompt 排在已启用技能指令之前，并纳入会话字符预算。
- 多模型预设切换同时应用连接信息、system prompt 和生成参数。
- 设置窗口新增“生成参数”页，模型配置窗口同步提供相同字段；已修正模型列表选择行
  的 1 像素布局越界代码。
- 增加生成设置、匿名访问、直连代理、HTTP MCP、工具回合参数、界面布局等自动测试
  源码和测试入口。
- 离线包加入 `Verify-FilePromptAI.exe`，用于检查包文件集合、Windows 7 SP1、
  .NET Framework 4.8、1920x1080@96 DPI、真实程序启动、回环模型接口、文件读取和
  多格式导出。
- 增加发布 ZIP 摘要、候选 receipt、封存脚本、annotated 标签复核及相应负向测试
  源码。
- 更新版本号、README、离线说明和功能矩阵至 v1.17 候选口径。

## 未完成或未验证

- **未在本次提交前重新运行构建、完整 smoke tests 或截图测试。** 当前提交状态以源码
  为准，不能把此前中间工作树上的结果视为最终提交证据。
- **尚未在真实 Windows 7 SP1 + .NET Framework 4.8、1920x1080@96 DPI 机器上运行
  最终包内的 `Verify-FilePromptAI.exe` 并取得退出码 0。** 因此不能声称 Windows 7
  已验收。
- 已知 `ModelProfileStore.Load` 在配置文件被其他进程独占锁定时，可能把暂时无法读取
  的有效文件误当成损坏文件并移动为 `.corrupt-*`；该竞态尚未修复。
- 设置窗口的 1 像素布局修正已写入源码，但按本次“直接提交、不验证”的要求尚未复测。
- 尚未在最终提交上重新构建 `FilePromptAI-Win7-Full-v1.17.zip`，现有本地产物不得作为
  本提交的正式发布包。
- 发布封存脚本目前绑定完整测试 receipt 与 ZIP，但尚未把真实 Win7 PASS 报告设为
  脚本级强制输入并绑定到同一包内容身份。
- 尚未生成并提交最终 `RELEASE-SHA256.txt`，未创建 annotated `v1.17` 标签，也未创建
  GitHub Release 或上传正式 ZIP。

## 发布限制

本候选提交可以推送供继续开发和审查，但在上述 Windows 7 验收、完整回归、最终包
重建及封存步骤完成前，不应创建 `v1.17` 标签或对外宣称 v1.17 正式发布。
