# FilePrompt AI v1.17 候选状态

更新时间：2026-08-24

本文件记录当前提交的实际完成边界。本提交是 **v1.17 候选源码**；它不是正式发布
声明，也不代表已经通过 Windows 7 目标机验收。

## 已完成并在当前工作树验证

- 可从用户填写的 OpenAI Chat Completions 兼容地址推导并主动请求同源 `/models`，
  对结果去重、排序并提供下拉选择，同时保留手工输入模型名称。
- API Key 可留空；匿名模型发现、连接测试和聊天请求不发送 `Authorization`。
- 模型请求、模型列表请求和 HTTP MCP 显式禁用 Windows 系统代理，只连接用户填写
  或由该地址推导出的端点。
- 新增普通 system prompt，以及可独立启停的 `temperature`、`top_p` 和最大输出
  token；设置和模型预设均可保存这些字段。
- 自定义 system prompt 排在已启用技能指令之前，并纳入会话字符预算。
- 多模型预设切换同时应用连接信息、system prompt 和生成参数。
- 主设置、模型预设和技能/MCP 配置在临时锁定、权限或 I/O 异常时进入粘性只读
  保护；不会把暂时不可读的有效文件误移为损坏文件，也不会用空配置覆盖原文件。
- 主界面固定为左侧会话、右侧上方聊天记录和底部消息编辑器；连接、技能/MCP、
  会话维护和卸载功能集中到独立设置窗口。文件选择、拖放、粘贴和路径添加均位于
  编辑器附件入口，失败路径保留到路径窗口供重试。
- 新增 PPTX 输入提取，以及新版 `content.json` 和旧版 `content.xml` XMind 输入提取；
  压缩包条目数、展开量、压缩比、路径、XML/JSON 大小和 XML 实体均有本地安全限制。
- 最新回复和整个会话均可导出 Markdown、文本、Word、PDF、PowerPoint 和 XMind；
  整个会话导出保留用户主动添加后进入历史的附件正文。
- 设置窗口新增“生成参数”页，模型配置窗口同步提供相同字段；已修正模型列表选择行
  的 1 像素布局越界代码。
- 增加生成设置、匿名访问、直连代理、HTTP MCP、工具回合参数、界面布局等自动测试
  源码和测试入口。
- 离线包加入 `Verify-FilePromptAI.exe`，用于检查包文件集合、Windows 7 SP1、
  .NET Framework 4.8、1920x1080@96 DPI、真实程序启动、回环模型接口、文件读取和
  多格式导出。
- 增加发布 ZIP 摘要、schema 2 候选 receipt、强制真实 Win7 PASS 报告的封存脚本、
  annotated 标签复核及相应正负向测试源码；三者以包清单原始字节 SHA-256 和条目数
  绑定同一最终离线包内容身份。
- 更新版本号、README、离线说明和功能矩阵至 v1.17 候选口径。

## 当前机器验证结果

- `tests/RunAllSmokeTests.ps1 -Version 1.17` 已通过 26 组测试及离线包重建；应用、
  API、网络、MCP、会话、输入提取、全部导出、UI、包清单、验收器和卸载安全均通过。
- 900x540 最小窗口和 1280x760 常规窗口截图已复核，左侧会话、右侧聊天记录和底部
  编辑器完整可见，附件入口不在顶部，控件无重叠。
- Windows Defender 已对最终候选 staging 目录和 ZIP 执行自定义扫描，签名库
  `1.457.310.0`，FilePrompt AI 相关检出为 0。
- 当前机器是 Windows 10 且为 125% 缩放；包内验收器按设计返回退出码 5，并明确拒绝
  生成 Win7 PASS。除操作系统和显示条件外，包清单、提取、导出、回环 API、真实启动
  和清理检查均通过。

## 未完成或不能在当前机器替代验证

- **尚未在真实 Windows 7 SP1 + .NET Framework 4.8、1920x1080@96 DPI 机器上运行
  最终包内的 `Verify-FilePromptAI.exe` 并取得退出码 0。** 因此不能声称 Windows 7
  已验收。
- 当前机器可执行的构建、专项和完整 smoke tests 需在每次候选提交前重新运行；这些结果
  只能证明当前环境的代码和离线包行为，不能替代上一条真实 Windows 7 报告。
- 当前顶层 `exe` 仍保留已正式验收的上一发布包；候选源码构建产物不得在真实 Win7
  验收前覆盖正式交付目录。
- 发布封存与标签复核的脚本级证据链已实现，但尚未取得真实 Windows 7 最终 PASS
  报告，因而仍不能执行正式封存或创建发布标签。
- 尚未生成并提交最终 `RELEASE-SHA256.txt`，未创建 annotated `v1.17` 标签，也未创建
  GitHub Release 或上传正式 ZIP。

## 发布限制

本候选提交可以推送供继续开发和审查，但在上述 Windows 7 验收、完整回归、最终包
重建及封存步骤完成前，不应创建 `v1.17` 标签或对外宣称 v1.17 正式发布。
